id: this               # 客户端ID
server: 127.0.0.1:6154 # 服务器地址
dashboard: # web面板
  enabled: true   # 是否开放dashboard
  listen: 0.0.0.0 # 监听地址
  port: 8080      # 监听端口号
#include conf.d/*.yaml
rules: # rule列表
  #include rule.d/*.yaml